# Build docker
docker build -f "Dockerfile-dev" -t sendiagustian/protonema/bs/oms-web .

# Test run local container
docker container create --name bs-oms-web --publish 8001:8001 sendiagustian/protonema/bs/oms-web

# untuk masuk container
docker exec -it bs-oms-web sh

# delete image <none>
docker rmi $(docker images --filter "dangling=true" -q --no-trunc)